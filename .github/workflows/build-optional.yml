---
name: Optional build test
on:
  workflow_dispatch:
  push:
    branches:
      - main
jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        os: ["ubuntu-latest", "macos-latest"]
        python-version: ["3.3", "3.4", "3.5", "3.6", "pypy-3.6", "pypy-3.7", "pypy-3.8", "pypy-3.9"]
        exclude:
          - os: macos-latest
            python-version: "3.3"  # not available for macos
          - os: macos-latest
            python-version: "3.4"  # not available for macos
          - os: macos-latest
            python-version: "pypy-3.6"  # does not install
    runs-on: ${{ matrix.os }}
    steps:
      - name: Check out repository code
        uses: actions/checkout@v2
        with:
          fetch-depth: 0
          submodules: true
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: ${{ matrix.python-version }}
      - name: Information
        run: |
          python3 -V
          python3 -W ignore -m pip --disable-pip-version-check list
          bin/skonfig -V
      - name: Build
        run: |
          python3 setup.py build
      - name: Install
        run: |
          python3 setup.py install
      - name: Clean
        run: |
          python3 setup.py clean --all
